import { defineConfig } from 'astro/config';

// Update `site` when the production domain changes. Astro uses it for canonical URL helpers.
export default defineConfig({
  site: 'https://thalcor.ca',
});
